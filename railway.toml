[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn nextoken.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300

[deploy.environment]
PYTHON_VERSION = "3.11.0" 